<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Crm" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes Tailwind CSS via CDN (temporary) %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-blue-600">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex h-16 items-center justify-between">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <%= link_to "CRM", root_path, class: "text-white text-2xl font-bold" %>
            </div>
            <div class="hidden md:block">
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "Dashboard", root_path, class: "text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Companies", companies_path, class: "text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Contacts", contacts_path, class: "text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Deals", deals_path, class: "text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Activities", activities_path, class: "text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Notes", notes_path, class: "text-white hover:bg-blue-700 px-3 py-2 rounded-md text-sm font-medium" %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <main>
      <div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
        <% if notice %>
          <div class="mb-4 rounded-lg bg-green-50 p-4 text-green-800">
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="mb-4 rounded-lg bg-red-50 p-4 text-red-800">
            <%= alert %>
          </div>
        <% end %>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
